# Quality tools — C# .NET MAUI Cross-Platform Application Scenario 1

All 12 alt tools live here. Shared configs in `config/`, runners in `scripts/`, outputs in `reports/<tool>/`.
MiniCover coverage XML feeds diff-cover. Missing tool binaries fail closed.
